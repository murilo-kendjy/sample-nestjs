import { NotFoundInterceptor } from './not-found.interceptor.js';

describe('NotFoundInterceptor', () => {
  it('should be defined', () => {
    expect(new NotFoundInterceptor()).toBeDefined();
  });
});
